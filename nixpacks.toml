# Adiciona o ffmpeg ao ambiente (extração de frames + áudio de vídeos no /api/ingest).
# "..." mantém os pacotes autodetectados (Node) e acrescenta o ffmpeg ao PATH.
[phases.setup]
nixPkgs = ["...", "ffmpeg"]

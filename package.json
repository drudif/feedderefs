{
  "name": "feedderefs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Catálogo de referências (site + servidor: auto-save, proxy Gemini, edição com senha no deploy).",
  "scripts": {
    "start": "node server.mjs"
  },
  "engines": {
    "node": ">=20.12"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.900.0"
  }
}
